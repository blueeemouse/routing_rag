# GraphRAG 性能异常分析指南

## 问题描述

**现象：**
- 用 hotpot_1000 前15条构建索引，测试15条，效果还可以（比no_rag好）
- 用 hotpot_dev_distractor 1000条构建索引，测试1000条，效果很差
- 但是，用 hotpot_dev_distractor 索引，测试 hotpot_1000 前15/100条，效果反而比测试 hotpot_dev_distractor 好

## 分析步骤

### 第1步：数据集特征对比

**目标：** 确认两个数据集是否有本质差异（难度、分布等）

```bash
python compare_datasets.py
```

**关注点：**
- 问题长度分布差异
- 答案复杂度差异
- 问题类型（单跳/多跳）分布
- 是否存在数据质量问题

---

### 第2步：索引质量检查

**目标：** 检查索引构建是否正确

```bash
python check_graphrag_index.py
```

**关注点：**
- 两个索引的实体数量是否与文档数量匹配
- 关系数量是否合理
- 索引文件大小是否正常
- 是否有异常的空文件或错误文件

**可能的问题：**
- 索引参数配置不同（导致实体提取不一致）
- embedding模型不一致
- GraphRAG版本或配置差异

---

### 第3步：检索结果质量分析

**目标：** 对比不同场景的检索结果

**需要检查的指标：**

1. **检索上下文长度**
   - 场景A (15+15): 平均检索token数
   - 场景B (1000+1000): 平均检索token数
   - 场景C (1000+15): 平均检索token数

2. **检索实体数量**
   - 是否大规模索引时检索了更多/更少实体

3. **检索关系数量**
   - 关系数量是否合理

4. **检索耗时**
   - 大规模索引是否显著增加检索时间

**如何获取这些数据：**
- 在GraphRAG执行代码中添加日志，打印每次检索的上下文长度、实体数、关系数
- 对比前10个问题在不同场景下的检索结果

---

### 第4步：手动对比检索结果

**目标：** 人工评估检索结果的相关性

**方法：**
```python
# 从日志中提取检索结果，对比示例
# 找一个具体问题，看三种场景下的检索内容
```

**示例问题：**
```
Which magazine was started first Arthur's Magazine or First for Women?
```

**对比维度：**
- 场景A (15+15) 检索到的关键实体是什么？
- 场景B (1000+1000) 检索到的关键实体是什么？
- 场景C (1000+15) 检索到的关键实体是什么？
- 哪个场景检索到了答案相关的关键信息？

---

## 可能的根本原因

### 1. 数据集难度差异

**假设：** hotpot_dev_distractor 比 hotpot_1000 更难

**验证方法：**
- compare_datasets.py 的结果
- 查看 dev_distractor 是否有更多复杂问题

**如果确认：**
- 这是正常的，索引本身没问题
- GraphRAG 在简单问题上表现较好

---

### 2. 索引构建参数不一致

**假设：** 两次构建索引时使用了不同的配置

**验证方法：**
- 检查两次运行时的配置文件（YAML）
- 对比 graphrag_hotpotqa_config.yml

**可能的不同参数：**
- `chunk_size`（影响文档切分）
- `community_detection` 算法/参数
- `embedding_model`
- GraphRAG版本

**如果确认：**
- 使用相同配置重新构建索引
- 确保环境一致

---

### 3. 检索算法在大规模数据上的问题

**假设：** GraphRAG的检索算法在小规模数据上正常，但大规模数据时有bug

**验证方法：**
- 对比场景A (15) vs 场景B (1000) 的检索行为
- 检查检索代码是否有与规模相关的bug

**可能的问题：**
- top_k设置在大规模数据时失效
- community遍历逻辑有bug
- embedding相似度计算在大规模时有问题

**如果确认：**
- 需要修复检索算法
- 或调整检索参数

---

### 4. 上下文长度问题

**假设：** 大规模索引时检索到的上下文太长/太短，影响LLM回答

**验证方法：**
- 打印三种场景下的检索上下文token数
- 对比LLM输入token数

**可能的情况：**
- 大规模索引检索到太多噪声，稀释了关键信息
- 或者检索到太少信息，缺少关键内容

**如果确认：**
- 调整检索的top_k参数
- 添加检索结果过滤（相关性阈值）

---

### 5. 索引质量问题

**假设：** 大规模索引的构建过程有问题，导致部分数据未正确索引

**验证方法：**
- check_graphrag_index.py 的结果
- 检查日志中是否有错误/警告

**可能的问题：**
- 构建过程中断，部分数据未处理
- 内存不足导致索引不完整
- 并发问题导致数据丢失

**如果确认：**
- 重新构建索引
- 增加构建时的日志输出

---

## 分析优先级

1. **高优先级（必做）：**
   - 运行 compare_datasets.py
   - 检查两次索引构建的配置文件
   - 打印前几个问题的检索结果（三种场景对比）

2. **中优先级（推荐）：**
   - 检查索引质量
   - 分析检索上下文长度分布
   - 手动评估检索结果相关性

3. **低优先级（可选）：**
   - 深入分析GraphRAG源码
   - 测试不同规模（100/500/1000）的索引性能

---

## 快速诊断命令

```bash
# 1. 对比数据集
python compare_datasets.py

# 2. 检查索引质量（需指定正确的索引路径）
python check_graphrag_index.py

# 3. 分析检索质量
python analyze_graphrag_retrieval.py

# 4. 查看GraphRAG日志
grep -i "error" graphrag_ollama_hotpotqa_test_data/*.log
grep -i "warning" graphrag_ollama_hotpotqa_test_data/*.log

# 5. 对比配置文件
diff graphrag_index_hotpotqa_1000_samples/graphrag_hotpotqa_config.yml \
     graphrag_ollama_hotpotqa_test_data/graphrag_hotpotqa_config.yml
```

---

## 预期发现

基于现象，最可能的原因是：

1. **检索参数问题（60%概率）**
   - 大规模索引时检索了太多噪声
   - 或者社区检测算法在大规模数据上有问题

2. **数据集难度差异（30%概率）**
   - dev_distractor 本身更难，需要更复杂的检索策略

3. **索引构建不一致（10%概率）**
   - 两次构建使用了不同配置

**下一步建议：**
1. 先运行 compare_datasets.py 确认数据集差异
2. 添加详细日志，打印检索结果
3. 手动对比前几个问题的检索质量
